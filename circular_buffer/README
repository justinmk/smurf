To compile and run sample tests on cbuff.c:
    gcc -I ../cmockery/ -Wall test_driver.c cbuff.c ../cmockery/cmockery.c && ./a.exe 9


test_driver.c:
    Contains a main() entry point which exercises the circular buffer and prints 
    debugging output to stdout.
    
